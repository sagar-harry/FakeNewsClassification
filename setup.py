from setuptools import setup, find_packages

setup(
    name="src",
    version="0.0.1",
    description="Fake news classification model",
    author="Konni Vidya Sagar",
    packages=find_packages()
)